# macOS system files that might cause issues
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon

# Thumbnails
._*

# Files that might appear on external disk
.Spotlight-V100
.Trashes

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Ignore node_modules directory
node_modules/

# Optionally, you can also ignore any nested node_modules directories
**/node_modules/

# If you're using npm v7 or later, you might also want to ignore package-lock.json
# Since npm v7, package-lock.json is automatically managed and can be ignored in repositories
package-lock.json

# If you're using Yarn, you might want to ignore yarn.lock
# Similar to package-lock.json, yarn.lock can be ignored
yarn.lock
